# ============================================================================
# Tool-Klipper-Calibration: Safe Staging Macros (Consolidated into macros.cfg)
# ============================================================================
# Toàn bộ macros dạy toạ độ an toàn (TEACH_CAMERA_SAFE_Z, TEACH_CAMERA_TARGET...)
# hiện đã được gộp trực tiếp vào macros.cfg.
# File này để trống nhằm tránh lỗi "gcode_macro already registered" trong Klipper
# khi các cấu hình printer.cfg cũ vẫn còn dòng [include safe_staging_macros.cfg].
# ============================================================================
