# ============================================================================
# Tool-Klipper-Calibration: Legacy Macro Entrypoint (Deprecated Stub)
# ============================================================================
# Tất cả macros căn chỉnh và điều hướng đã được gộp trực tiếp vào:
#   tool_calibrator.cfg
#
# File này được giữ lại dưới dạng rỗng (no-op stub) để đảm bảo nếu printer.cfg
# của bạn vẫn còn dòng [include tool_calibrator_macros.cfg] thì Klipper vẫn
# khởi động bình thường mà không bị lỗi trùng lặp macro hay file không tồn tại.
#
# Bạn có thể xóa dòng [include tool_calibrator_macros.cfg] trong printer.cfg.
# ============================================================================
