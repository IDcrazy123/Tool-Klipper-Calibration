# ============================================================================
# Tool-Klipper-Calibration: Macro Stub
# ============================================================================
# Tất cả macros căn chỉnh và điều hướng đã được gộp trực tiếp vào:
#   tool_calibrator.cfg
#
# File này để trống nhằm tránh lỗi "gcode_macro already registered" trong Klipper.
# Bạn chỉ cần include duy nhất:
#   [include tool_calibrator.cfg]
# ============================================================================
